extends layouts/main

block content
    .form-container--join
        form(action=routes.join, method="post")
            .form__form
                h2.logo
                    span.blue G
                    span.red o
                    span.yellow o
                    span.blue g
                    span.green l
                    span.red e
                h2 YouTube 계정 만들기
                input(type="text", name="name", placeholder="이름",required=true)        
                input(type="email", name="email", placeholder="이메일",required=true)
                .password
                    input(type="password", name="password", placeholder="비밀번호",required=true)
                    input(type="password", name="password2", placeholder="확인",required=true)
                input.button(type="submit",value="가입")
                include partials/socialLogin
            .form__img
                img.google(src="https://ssl.gstatic.com/accounts/signup/glif/account.svg", alt="")
                span.form__img__text 하나의 계정으로 모든 Goolgle 서비스를 이용할 수 있습니다
